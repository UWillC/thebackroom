# Dockerfile for The Backroom MCP Server
FROM python:3.11-slim

WORKDIR /app

# Install dependencies (pinned: an unpinned rebuild on 2026-09-11 jumped fastmcp 3.4.4 -> 4.0.3)
RUN pip install --no-cache-dir fastmcp==4.0.3 supabase==2.31.0

# Copy all server code (modular structure)
COPY server.py .
COPY auth/ ./auth/
COPY utils/ ./utils/
COPY core/ ./core/
COPY enterprise/ ./enterprise/
COPY x/ ./x/

# Environment variables
ENV MCP_TRANSPORT=http
ENV HOST=0.0.0.0
ENV PORT=8000

EXPOSE 8000

CMD ["python", "server.py", "--http"]
